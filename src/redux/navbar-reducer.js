let initialState = {
    friends: [
        {id: 0, name: 'Jenya'},
        {id: 1, name: 'Sasha'},
        {id: 2, name: 'Tolya'}
    ]
};

const navbarReducer = (state = initialState, action) => {
    
    return state;
}

export default navbarReducer;